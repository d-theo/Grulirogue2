import { lineTile } from "../map_tiling";
import { pointsOfRect } from "../map-geo";
import { randomIn } from "../../game/utils/rectangle";

export function snakeBoss(room, tilemapBg, tilemap) {
    
}